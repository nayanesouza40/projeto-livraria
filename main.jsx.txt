import { createBrowserRouter, RouterProvider, Route } from "react-router-dom";

import App from "./App";

import Home from "./routes/Catalogo";
import Post from "./routes/Livro";

const router = createBrowserRouter([
  {
    element: <App />,
    children: [
      {
        path: "/livro/:id",
        element: <Livro />,
      },
      {
        path: "/catalogo",
        element: <Catalogo />,
      }
    ],
  },
]);